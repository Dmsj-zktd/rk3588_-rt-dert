## AI嵌入式C++代码注释规范

### 1. 基础语法规则

| 类型     | 符号        | 适用场景                                             |
| -------- | ----------- | ---------------------------------------------------- |
| 多行注释 | `/* ... */` | 仅在文件头版权声明或极少量临时大段注释禁用代码时使用 |
| 单行注释 | `//`        | **所有源代码注释（推荐）**，包括独立行和行尾         |

**强制约定**：
- `//` 后必须紧跟**一个空格**，再写内容。
- 行尾注释：代码与 `//` 之间至少**两个空格**。
- 注释首字母建议大写（英文），中文可灵活处理，但须保持团队统一。

---

### 2. 分场景注释风格

#### 2.1 头文件（.h）——公共接口使用 **Doxygen风格**

用于类、函数、结构体、枚举、宏等对外接口的声明。

**格式**：`/**` 开头，`*/` 结尾，使用 `@brief`、`@param`、`@return`、`@note` 等标签。

**示例**：
```cpp
/**
 * @brief 初始化电机控制器
 * @param pwm_freq  PWM 频率，单位 Hz
 * @param dead_time 死区时间，单位微秒
 * @return 0 表示成功，负数表示错误码
 * @note 调用前需确保外设时钟已使能
 */
int motor_controller_init(uint32_t pwm_freq, uint16_t dead_time);
```

#### 2.2 源文件（.cpp）——实现细节使用 **独立行注释** 或 **行尾注释**

- **独立行注释**：解释上方代码块的功能、算法步骤、注意事项。
  ```cpp
  // Check if the FIFO has enough space before pushing
  if (fifo_available() > 0) { ... }
  ```

- **行尾注释**：解释单行变量、常量、寄存器位域等的含义。
  ```cpp
  uint16_t last_angle_raw;  // raw angle from encoder, 0~8191
  ```

#### 2.3 特殊标记注释（项目管理 / 待办事项）

全部使用**大写**，建议后跟责任人（可选）和简明描述。

| 标记       | 含义                                 |
| ---------- | ------------------------------------ |
| `// TODO`  | 功能未完成，后续需补充或优化         |
| `// FIXME` | 已知缺陷或潜在Bug，需修复            |
| `// NOTE`  | 特别注意点，如硬件约束、性能要求     |
| `// XXX`   | 此处代码临时、hack或危险，需更优方案 |

**示例**：
```cpp
// TODO(zhangsan): add watchdog reset after long idle
// FIXME: interrupt nesting may cause priority inversion
// NOTE: this loop must finish within 50us
// XXX: hardcoded magic number, should derive from MCU clock
```

---

### 3. AI友好注释原则（核心）

注释应补充代码**无法表达**的信息，优先回答 **Why（为什么）** 和 **What for（为了什么）**，而非重复 **What（做了什么）**。

| 原则                  | 解释                                         |
| --------------------- | -------------------------------------------- |
| **说明意图**          | 解释逻辑目的，而非翻译语句                   |
| **记录硬件约束**      | 时序、引脚电平、外设限制、噪声规避等         |
| **记录业务/算法约束** | 状态机跳转条件、滤波参数选取原因、安全边界   |
| **记录非功能需求**    | 实时性（执行时间）、内存占用、中断优先级限制 |

**示例对比**：

| ❌ 差（只描述What）          | ✅ 好（解释Why / What for）                               |
| --------------------------- | -------------------------------------------------------- |
| `cnt++; // 计数器加1`       | `cnt++; // 记录已发送的CAN报文总数，用于心跳检测`        |
| `delay_us(20); // 延时20us` | `delay_us(20); // 等待RS485收发器方向切换完成`           |
| `if (adc > 3000) ...`       | `if (adc > 3000) // 对应电流 > 10A，触发过流保护`        |
| `// 初始化定时器`           | `// 定时器4用于生成PWM，频率=20kHz，占空比由PID输出控制` |

---

### 4. 约束规则（强制/推荐）

#### 4.1 必须注释的内容
- 所有**公开接口**（函数、类、结构体、枚举、全局变量）——使用Doxygen。
- **复杂算法**（滤波、PID、状态机、数学变换）——概述步骤或公式出处。
- **非显而易见的寄存器/位域操作**——说明含义或参考手册章节。
- **临界区、中断相关**——说明关闭/恢复中断的原因和时机。
- **硬编码常数**——说明来源（如时钟频率、硬件电气参数）。

#### 4.2 禁止的做法
- ❌ 无意义注释：`int a; // 定义整数a`
- ❌ 过期注释：修改代码后必须同步更新注释。
- ❌ 大段被注释掉的代码 —— 应直接删除，需要时通过版本控制恢复。
- ❌ 中文英文混杂（除非团队约定允许）。

#### 4.3 例外文件（免注释/低注释）
- 芯片厂商提供的CMSIS、HAL驱动库
- 第三方RTOS内核及协议栈
- 自动生成的代码（如CubeMX配置）

> **理由**：这些文件不属团队维护范围，AI生成注释可能引入误导信息。

---

### 5. 给AI的提示词模板（用于代码生成/审查）

当你使用AI辅助写代码或添加注释时，可在指令中明确：

```
请按以下规则为C++代码添加注释：
1. 头文件接口使用Doxygen风格，标注@brief, @param, @return。
2. 实现文件使用//单行注释，重点解释算法意图、硬件约束和注意事项。
3. 不要为简单赋值/自增等琐碎代码加注释。
4. 对硬件相关操作注明时序、电平、外设要求。
5. 禁止修改/添加任何厂商库或第三方代码的注释。
```

---

### 6. 速查卡片（记忆版）

| 场景               | 风格                      | 格式示例                                             |
| ------------------ | ------------------------- | ---------------------------------------------------- |
| 函数声明（.h）     | Doxygen                   | `/** @brief ... @param ... @return ... */`           |
| 类/结构体（.h）    | Doxygen                   | 同上                                                 |
| 代码块说明（.cpp） | 独立行 `//`               | `// Wait for ADC conversion complete`                |
| 单行变量/常量      | 行尾 `//`                 | `int timeout_ms = 100; // max waiting time`          |
| 待办/警告          | 标记 `// TODO/FIXME/NOTE` | `// FIXME: overflow possible when speed > 3000`      |
| AI协作提示         | 强调Why + 硬件约束        | `// This delay ensures MOSFET gate fully discharged` |

