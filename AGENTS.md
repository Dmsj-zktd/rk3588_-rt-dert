# LKD3588 (RK3588) 硬件基本配置介绍

## 目标平台
- **SoC**: Rockchip RK3588（8nm，4×A76 + 4×A55 @ 2.4GHz）
- **内存**: 8GB LPDDR4（可用约 7.72GB）
- **内置存储**: 64GB eMMC 5.1
- **NPU**(3核): 6 TOPS，支持 int4/int8/int16/FP16/BF16/TF32
- **系统**：Ubuntu 22.04
- **补充**：有不清楚的细节，请查阅网址：[LKD3588](https://neardi.com/cms/index/productsd.html?id=5&title=LKD3588)或[e428abc5d0edb90bba87fdf6074394dc.pdf](https://neardi.com/uploads/files/20260312/e428abc5d0edb90bba87fdf6074394dc.pdf)

# AI嵌入式C++代码注释规范

该内容详见“AI嵌入式C++代码注释规范.md”文档，不过优先保持符合项目代码原有的统一注释风格。

# 板端访问与工作规范（必须时刻遵守）

## 板端访问
- 通过 SSH 访问板端：`ssh -i ~/.ssh/codex_board_ed25519 neardi@192.168.2.234`（端口 22）。
- 板端 workspace 根目录固定为 `/home/neardi/Workspace_Codex/`，**未经用户许可，不得访问或修改该目录之外的任何内容**。
- 板端尚未配置实际摄像头设备：**不检查、不依赖摄像头**。

## 板端项目与测试
- `/home/neardi/Workspace_Codex/` 下已配置好项目（含 git 仓库、测试视频/图片、RKNN 模型）。
- 默认测试素材：视频 `cars.mp4`、图片 `uav.jpg`。
- 当前 RKNN 模型输出**未做归一化**，置信度阈值参数的实际有效范围为 `> -1`（例如 `-c -0.13`），不要按 0~1 的常规区间假设。

## 工作流程
1. **先在板端本地项目上迭代**，达成特定指标需求后，再上传/提交仓库并同步本地。
2. 除非确有特殊需求（如必须本地查看的文件），优先通过 SSH 命令行直接查看效果，避免无谓的本地同步。
3. 遵守已约定的安全边界：非 root 用户操作、破坏性操作先征得许可、测试命令带 timeout、实验前 git 留底以便回滚。

# 任务管理与进度跟踪（必须遵守）

- 主任务统一记录在 `TODOlist.md`，**按编号顺序执行，前置任务完成并验证通过后才能开始下一个任务**。
- 每次取得进展/完成阶段时，必须同步动态更新 `TODOlist.md` 中的状态、完成时间、指标对比与备注，防止跨对话丢失上下文。
- 本规则已在 AGENTS.md 中启用，若任务列表更新，需同时回写 TODOlist.md。

# 指标对比与优化验证（必须遵守）

- 所有优化/改动都必须以**数据说话**：先定义指标、记录基线，改动后再测同一指标进行对比，确认有效才算完成。
- 指标与基线数据统一记录在 `METRICS.md`，定义如下（可按项目实际情况增补）：
  - 整体 FPS（处理帧数 / 总耗时）；
  - 各阶段平均耗时：预处理 / NPU 推理 / 后处理（μs，来自程序 PerfCounter）；
  - 进程 CPU 占用率：由 `/usr/bin/time -v` 的 User+System 时间与 Elapsed 推算；
  - 进程内存占用：Max RSS（KB），以及 DMA 池分配总量（来自程序日志）；
  - 检测质量：平均每帧检测目标数、有目标帧占比；
  - 图片模式（任务 1）：单图端到端耗时、输出图片检测框数量；
  - 硬编/硬解（任务 4）：编码/解码阶段 CPU 占比与耗时、输出文件大小与编码器类型。
- 指标基线必须使用**当前系统效果最佳参数**：`-p 2 -n 14 -P 3 -c -0.13f`。

# 启用的项目规范文档

- `uncle-bob-constraint-boundary.md`：**已启用**。代码变更必须遵守其约束体系：
  - 新增/修改代码必须配套单元测试并全部通过；
  - Gherkin 验收测试与 QA 流程由人类定义，Agent 只负责实现与执行；
  - 质量指标（圈复杂度、耦合度、重复率、依赖方向）与静态检查需执行并如实报告；
  - 变异测试通过率与覆盖率以文档阈值为目标（默认 ≥80% / ≥90%），**禁止伪造结果、禁止降低阈值、禁止静默忽略失败**；
  - 约束检查失败必须明确报告，不得以"稍后处理"为由跳过。
- `AI嵌入式C++代码注释规范.md`：注释规范，**注释风格优先与项目代码已有风格保持统一**。

# 上传入库与同步规则（必须遵守）

1. 每一次**实际达成所有限制和完成要求**的任务后，必须向用户发起询问：是否上传入库并同步本地。
2. 若用户**拒绝**本次上传：不提交、不推送，等待与下一个用户同意上传的任务一起入库。
3. 若用户**同意**上传：先同步更新 `README.md`（记录该任务的功能/指标/用法变更），再执行 git 提交（并推送），以便随时回溯归档。
4. 未获用户同意前，不得执行任何 git 提交/推送。

# 板端性能释放（每次 SSH 登录后必须自发执行）

```bash
sudo sh -c 'echo performance > /sys/devices/system/cpu/cpufreq/policy0/scaling_governor'
sudo sh -c 'echo performance > /sys/devices/system/cpu/cpufreq/policy4/scaling_governor'
sudo sh -c 'echo performance > /sys/devices/system/cpu/cpufreq/policy6/scaling_governor'
sudo sh -c 'echo performance > /sys/class/devfreq/fdab0000.npu/governor'
sudo sh -c 'echo performance > /sys/class/devfreq/dmc/governor'
```

> 注意：不写入系统自启，仅每次登录后由 Codex 手动执行。

# 当前系统效果最佳参数（指标对比基准）

- `-p 2 -n 14 -P 3 -c -0.13f`
- 模型输出未归一化，置信度阈值有效范围 `> -1`；`-0.13f` 为命令行写法，程序解析时忽略末尾的 `f`。

# 板端操作权限（必须遵守）

1. **只读类指令**（查看文件/状态、cat、ls、git status、日志等）无需询问，直接放权执行。
2. **编写/删改类指令**（创建/修改/删除文件、git 提交推送、安装依赖等）**必须先询问用户，获得许可后才能执行**，不可越界。
3. 其他必要的规则/指令/行为可自行设计，但**必须先过问用户**，经同意后才可写入 AGENTS.md 启用。
