## Skill: `uncle-bob-constraint-boundary`

### 一、Skill目标

本Skill定义了Agent在生成代码时必须遵守的自动化约束体系。**Agent的核心职责是：在约束边界内自主生成代码并通过所有自动化检查，不依赖人工逐行审查。** 人类仅在验收标准定义、QA流程设计和最终产品验证三个层面介入。

### 二、核心理念

> “在Agent周围设置极其严格的约束，包括单元测试、Gherkin测试、QA流程、质量指标、变异测试、测试覆盖率，以及大量其他检查机制。最终对生成的代码有很高的信心，因为这些代码必须通过重重约束与测试。”

**关键原则：**
- 信任从“审查代码”转移到“验证结果”
- 约束是自动化的，不可绕过
- Agent自主迭代直到通过所有约束

### 三、强制约束清单

Agent在提交任何代码变更前，**必须**确保以下所有约束通过：

#### 3.1 单元测试 (Unit Tests)
- Agent必须为所有新增/修改的代码编写单元测试
- 单元测试由Agent自主生成，**不经过人工审查**
- 测试必须全部通过

#### 3.2 Gherkin验收测试 (Gherkin Tests)
- 使用`Given/When/Then`格式编写验收测试
- **此部分由人类定义和审查**，Agent仅负责实现使测试通过的代码

#### 3.3 QA流程 (QA Procedures)
- 执行预定义的自动化QA检查清单
- **QA流程的定义和审查由人类负责**，Agent执行

#### 3.4 质量指标 (Quality Metrics)
- 代码复杂度（圈复杂度）
- 模块耦合度
- 代码重复率
- 依赖方向（遵循依赖规则）

#### 3.5 变异测试 (Mutation Testing)
- 对代码进行变异（修改逻辑），验证测试套件能否捕获变异
- 变异测试通过率必须达到阈值（默认≥80%）

#### 3.6 测试覆盖率 (Test Coverage)
- 行覆盖率、分支覆盖率必须达到阈值（默认≥90%）

#### 3.7 其他检查机制
- 静态代码分析（Lint）
- 安全漏洞扫描
- 性能基准测试

### 四、执行流程

```
1. 人类定义 → Gherkin验收测试 + QA流程规格
2. Agent执行 → 编写代码 + 单元测试
3. Agent迭代 → 运行所有约束检查
4. 若失败 → Agent自主修复 → 回到步骤3
5. 若全部通过 → 提交代码
6. 人类 → 定期最终人工测试（验证产品真实表现）
```

### 五、人类把控环节（不可自动化）

以下环节**必须由人类执行**，Agent不得僭越：

| 环节                | 说明                                                         |
| ------------------- | ------------------------------------------------------------ |
| **Gherkin测试定义** | 验收标准的编写和审查，Agent只负责实现                        |
| **QA流程设计**      | QA检查清单的定义和审查                                       |
| **审查强度分级**    | 根据项目风险决定审查强度：关键功能全面审查，普通功能抽查     |
| **最终人工测试**    | 定期在真实环境中验证产品表现                                 |
| **规格偏差检测**    | 当Agent“为暂时跳过某项任务寻找理由，承诺稍后处理”时，人类需介入纠正 |

**人类检查频率建议：**
- 高風險项目：每轮迭代后审查Gherkin测试和QA流程
- 中等风险项目：每3-5轮迭代审查一次
- 低风险项目：每10轮迭代抽查一次

### 六、Agent行为约束规则

1. **禁止修改Gherkin测试**：Agent不得擅自修改人类定义的验收测试
2. **禁止伪造测试结果**：不得制造假阳性通过
3. **禁止降低覆盖率阈值**：不得以任何理由绕过覆盖率要求
4. **必须报告失败**：所有约束检查失败必须明确报告，不得静默忽略
5. **禁止“稍后处理”承诺**：不得为跳过某项任务寻找理由并承诺稍后处理
