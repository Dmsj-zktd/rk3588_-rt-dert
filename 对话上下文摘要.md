# 对话上下文摘要（2026-08-15 更新，供后续对话续接）

## 项目与环境

- RK3588（LKD3588）端侧目标检测：RT-DETR-R18 INT8，C++17，CMake，RGA/DMA 零拷贝，Ubuntu 22.04。
- 本地 workspace：`C:\Users\zky\Desktop\experimental code 1\毕设-RK\【毕设】-RK3588\rt-detr-rk3588\Dmsj_毕设`（源码/文档均为 UTF-8）。
- 板端：`ssh -i ~/.ssh/codex_board_ed25519 neardi@192.168.2.234`；workspace 根 `/home/neardi/Workspace_Codex/`（写操作仅限该目录）；原版参考系统 `/home/neardi/Workspace_Dmsj/DMSJ_Design/rt-detr-rk3588-deploy/`（只读/执行授权）。
- 每次 SSH 登录后必须执行 5 条 performance governor 命令（AGENTS.md「板端性能释放」）；**板子重启会重置 governor**。
- 模型：`models/RT-DETR-RK3588-Models/.rknn/rtdetr_i8.rknn`（10 类 VisDrone；输出未归一化，阈值有效范围 > -1）。
- 测试素材：`cars.mp4`(720p)、`uav.jpg`(1360×765)、`test_people_small_little_18s.mp4`(1080p)、`cars-from uav_Unconventional Size_.mp4`(480×332@60)、录屏 mp4(480×332)。
- 摄像头（2026-08-15 起可用）：`/dev/video41` = Web Camera (usb-xhci-hcd.4.auto-1.1)；MJPG 1920×1080@30 / 1280×720@30 等，YUYV 1920×1080@10 / 1280×720@15 等；另有 `/dev/video42`（同一设备，可能为元数据流）。

## 关键规则（AGENTS.md，必须遵守）

- 板端先迭代 → 达成指标后询问用户是否上传入库；同意则更新 README + git 提交推送；拒绝则与下个同意任务一起入库；未同意前不提交不推送。
- 指标驱动（METRICS.md），改动前后同指标对比；uncle-bob：新代码配套单测并如实报告（覆盖率/变异工具未配置，未伪造）。
- 权限：板端只读指令放权；编写/删改/安装必须先询问。
- 双基准参数：最佳性能 `-p 2 -n 14 -P 3 -c -0.13f`；显著降 CPU/内存甜点位 `-p 2 -n 8 -P 3 -c -0.13f`。
- SSH 引号/特殊字符注意事项见 `SSH交互注意事项.md`（执行 ssh 前必须自检）。

## 任务状态（TODOlist.md）

| # | 任务 | 状态 |
|---|------|------|
| 1 | 图片输入检测 + 检测一致性修复 | ✅ 已上传（e8308ab） |
| 2 | 日志等级化 `-G/--DEBUG` | ✅ 已上传（bc49c84） |
| 3 | RGA_DMA 链路分析优化（迭代 1+2） | ✅ 已上传（16d752f） |
| 4 | GStreamer + RK MPP 硬解/硬编（迭代 1+2+3+4） | ✅ **已上传入库**（2026-08-13，用户同意；README 已更新，git 提交并推送 gitee） |
| 4' | 任务4 迭代5：VFR 帧率误判修复 | ✅ 完成，**待用户确认上传**（2026-08-14；时长 9.2s→21.31s，单测 22/22） |
| 4'' | 任务4 迭代5 修复：CFR 误判 VFR | ✅ 完成，**待用户确认上传**（2026-08-14；CFR 零两遍、长视频立即开 writer） |
| 5 | 预处理/后处理提速（排除 NPU） | ⏸ **闲置（暂缓，2026-08-14 用户指令）** |
| 6 | **NPU 推理耗时瓶颈（大任务，优先级最高）** | ✅ **已收尾（2026-08-15）**：仅保留 Stage1，Stage2/3 回退、Stage4 放弃；未达 25-30 FPS，下一步待用户决策 |
| 7 | **摄像头通路验证 + draw_results 优化 + 实时播放（合集）** | ✅ **已完成并上传入库（2026-08-16，commit 08f20e5）**：7.1 摄像头通路；7.2 draw_results 优化；7.3 实时播放 |
| 8 | **--display 抖动/相机停流修复（任务8，实验性）** | ↩️ **已回退（2026-08-16）**：往返抖动修复有效，但用户反馈 720p 变卡/长视频 CPU 上升；源码备份 cpp_task08/，历史分支 task8-backup，master 回退 f2bd68c |
| 9 | **-F/--fps 输入/输出帧率覆盖** | ✅ **已完成并上传入库（2026-08-16，commit 505c0eb）**：视频文件输入限速+输出fps、camera 采集+输出fps 均随 -F；未指定保持默认，无基准性能影响 |

## 关键技术结论（含 2026-08-13 新增）

- Bug1：输出张量误选（4200 元素 pred_logits vs 3000 元素 logits）→ 形状匹配修复（300×4 / 300×10，最后匹配者胜）。
- Bug2：RGA 源 stride 对齐污染（1360 宽 → DRM stride 4096）→ mat 输入走 RGA virt→DMA；相机 DMA→DMA 用 wstride=实际 stride/bpp，非整除回退 CPU 逐行读取。
- 1080p 顶部绿条：mppvideodec 高度对齐 1080→1088，caps 只报 1080，UV 平面偏移 → 用 `GstVideoMeta` 真实 offset/stride 组装紧凑 NV12 + `arm-afbc=false`。
- RGA 并发 `Invalid argument`：`-p 2` 下原地 imcvtcolor 偶发失败（librga 2.2 并发缺陷）→ 全局互斥锁串行化（实测零开销），CPU 回退兜底。
- 特殊尺寸视频（480×332@60）模糊/慢放：容器帧率元数据缺失 → 读取端 PTS 推算帧率（60fps）；编码码率自适应 `bps = w×h×fps×0.2`（clamp 1~12Mbps）。
- 多格式支持：avi/mkv/webm/ts/hevc/h264 裸流/bmp/webp；H.264→H.265 自动重试 + 数据流验证（800ms 内须出帧）；OpenCV 回退。
- **GStreamer 无 RGA 插件**（无全局宏可开；RGA 硬件转换只能在自己的代码里做）。
- **特殊尺寸“仍模糊”根治（2026-08-13 迭代4）**：
  - 根因①：mpph264enc 把 appsrc 默认 sRGB 色彩学写入 H.264 VUI（`pc/gbr/sRGB`），解码按错误矩阵还原 → 对比度损失 ~13%；修复：appsrc caps 显式 `colorimetry=bt709` → 输出 `tv/bt709`，帧 std 47.4→恢复 47.5。
  - 根因②：默认 cbr+baseline 低码率块状伪影；修复：默认 **fixqp qp-init=22 + profile=high**（恒定质量），`set_encoder_params(rc,qp,profile)` 可配置；VBR 回退公式 `bps=w×h×fps×0.35`（clamp 2~20M）。
  - PTS 帧率估算改**众数统计**（VFR 抗抖动）；实测特殊视频仍判 60fps。
  - 挂起修复：NPU 模型加载失败 → 熔断丢帧 + wait_idle 30s 上限 + 析构先 shutdown 队列；回归测试 21/21。
  - 实测（-n8）：特殊 15.64FPS/328%/822MB/8.95Mbps；720p 15.38/333%/966MB/4.35Mbps；1080p 15.02/352%/1148MB/5.17Mbps；顶部绿条=0。
  - 产物：`img/自测小尺寸_-0.83f_gst_修复v4.mp4`（对照旧 `_gst_模糊.mp4`）。
- 图片模式：`-i/--image <图> -o <输出> -c 阈值`（与 `-v` 互斥）；日志：`-G 0`=仅错误+报告、`-G 1`=Main、`-G 8`=全部。
- 指标：最佳性能 ~15.8 FPS/430%/1500MB；甜点位 ~15.3 FPS/365%/953MB（软解）；硬解硬编后 CPU -10~12%、后处理 ~40→3ms；单测 **20/20**。
- **VFR 帧率误判修复（2026-08-14 任务4 迭代5）**：480×332 录屏 writer 打开时只取前两帧 PTS 间隔
  （前段 60fps burst）→ 误判 60fps → 输出 9.2s（源 21.7s）+ 快进闪烁观感；PTS 全量统计众数/中位数
  间隔 33ms（30fps）、平均 26.6fps。修复：GstVideoReader 新增 caps_fps_authoritative()/measured_avg_fps()，
  VFR（caps 0/1）两遍法（MPP 硬解全量扫描，probe 25.5fps）→ 输出 26fps/21.31s；CFR 零开销。
  实测：特殊 15.63FPS/321%/827MB（持平），全片抽样 0 花屏；720p/1080p 回归无变化；单测 22/22。
- **迭代5 修复（2026-08-14）**：CFR 视频被误判 VFR——caps_fps_valid 仅在 read() 置位而判断在
  open 后首帧前 → 全部走两遍；长视频（25fps/300s）先等 probe ~30s 才开 writer。修复：verify 出帧
  阶段直接从 sample caps 解析帧率；CFR 零两遍、直接用 caps fps（如 25）；VFR 仍两遍（25.5→26/21.3s）。
- **任务6 收尾（2026-08-15）**：Stage1 保留（输出索引缓存/输出预分配/FrameBundle 预分配，24/24 单测，
  n8 复测 15.27 FPS 无回退，提交 9fc657b）；Stage2 零拷贝输入、SRAM、topology、动态批处理均无收益/放弃。
- **任务7 摄像头（2026-08-15）**：`/dev/video41` Web Camera 已就绪；V4L2 零拷贝通路此前从未真机验证，
  需先验证 camera 链路（DMA→DMA 预处理 + 推理 + 画框）再测性能；允许输出无检测目标的 cases。
- **任务7.1 完成（2026-08-15）**：协商 YUYV 1280×720 stride 2560（源上限 ~10.8fps）；全链路打通，
  n8：10.82 FPS / CPU 118.7% / RSS 780MB / pre 1.95ms / npu 233ms / post 0.35ms（n14 同 FPS、RSS 1308MB）。
  关键修复：YUYV 协商链、相机 buffer 不提前 munmap/close、预处理后提前归还 src_buf（7.6→10.8 FPS）、
  采集对象生命周期（退出不再 core dump）、poll 优雅退出、默认不写视频；单测 26/26，视频回归无回退。
- **任务7.2/7.3 完成（2026-08-16）**：
  - draw_results：固定调色板 + 文字度量缓存 + 去 AA + 小目标细框不画字；线宽/字号随分辨率自适应；
    微基准 3.4~3.8x，流水线 post 视频 1.34→0.49ms、相机 0.35→0.013ms。
  - `--display` 实时播放：专用显示线程 + 丢旧保新队列；HDMI-2 实屏验证窗口 RT-DETR；
    视频 15.27 FPS、相机 10.81 FPS（显示 vs 无显示差 <1%）；q/ESC 退出。
  - 回归：单测 29/29；视频 n8 15.37 FPS / n14 15.67 FPS 无回退。
  - 上传入库：2026-08-16 用户同意，README 已更新，commit 08f20e5 已推送 gitee。

## 待办与操作建议

1. **任务 4 迭代 5 + 修复 待用户确认上传**（2026-08-14）：VFR 两遍法（时长 9.2s→21.31s）
   与 CFR 误判修复（零两遍、长视频立即开 writer，单测 22/22）；同意后更新 README → git 提交推送。
2. **任务 6 已收尾（2026-08-15）**：保留 Stage1（9fc657b，待上传确认），Stage2/3/4 回退/放弃；
   25-30 FPS 目标未达成，下一步方向（动态批处理重做/换模型/其他）待用户决策。
3. **任务 4-5（动态尺寸 RGA）已实测并回退（2026-08-14）**：大尺寸非 16 对齐源（1640×2360/2360×1640）
   上 FPS -3~6%、RSS +4~6.5%（RGA 硬件 resize 慢于 CPU NEON、编码器 BGRA 路径慢于原 CPU 回退），
   按用户规则放弃本次修改，代码/文档全部回到 `69e3034`（任务4 迭代1-4，单测 21/21，cars3 FPS 恢复 14.52）。
   RGA_BLIT fail 噪音为上游 gst-mpp stride 缺陷 + RGA 带宽问题，留待后续专项研究。
4. **任务 4 已上传入库**（2026-08-13）：README 已更新并推送 gitee；如需后续回溯可查该 commit。
5. 可选迭代（建议优先级，含零拷贝方案评估；NPU 瓶颈任务启动前不执行）：
   - RGA NV12→BGR 硬件转换（代码内 `imcvtcolor`，CPU 再降 ~1-2%）——中收益、中风险；
   - mppvideodec DMABuf → RGA → NPU 零拷贝深度集成——**建议暂缓**（NPU 为瓶颈 FPS 不升；仅省 cvtColor+memcpy ~2-4% CPU；无摄像头无法验证 DMA→DMA 半程；留待任务5 后评估）；
   - 视频帧环形缓冲消除 `orig_img.clone()`——低优先级（用户已认可缓做）。
6. 板端 git 本地已提交未推送：cfb4ee7（任务4 迭代5+修复）、9fc657b（任务6 Stage1），均待用户确认上传。
7. 每次 ssh/scp 前对照 `SSH交互注意事项.md` 自检；板子重启后先设 performance；**测试命令一律加 timeout/看门狗**（本次模型路径错误曾导致挂死）。
8. 任务7 执行顺序：7.1 摄像头通路 → 7.2 draw_results 优化 → 7.3 实时播放；每步完成后回写 TODOlist/METRICS。
9. 本文件（对话上下文摘要）已恢复为最新状态；上传时由用户决定是否入库。

### 任务8 回退记录（2026-08-16）

- 任务8 曾提交推送（beb4a05/66aa913）：显示重排序防往返抖动 + V4L2 立即归还 + 相机实时丢帧等，实测有效。
- 用户反馈后复核：-p 2 -n 14 -P 3 长视频非显示 FPS 15.82~15.97、CPU ~430%（≈54%/核）、30帧≈1.9s，
  显示态 Xorg 仅 1~3%、backwards=0 —— 未复现 88%/3s；仍按用户指令回退。
- 回退结果：源码备份本地 `cpp_task08/`；git 分支 `task8-backup`（beb4a05）；master 回退 `f2bd68c` 并重新推送；板端同步。

### 任务9 完成（2026-08-16，-F/--fps 帧率覆盖）

- 未指定 -F：输入/输出 fps 保持原有默认（视频=源 fps、camera=30）。
- 指定 -F：视频文件输入按 -F 限速喂帧、输出 writer 用 -F；camera 采集请求与输出均用 -F；跳过 VFR 两遍法。
- 验证：-F 10 → 输入 9.68fps/输出 10fps；无 -F → 输出 30fps；camera -F 15/30 → 输出 15/30fps；基线回归 15.23 FPS/post 0.51ms 无变化；单测 29/29。
  - 上传入库：2026-08-16 用户同意，commit 505c0eb 已推送 gitee。
