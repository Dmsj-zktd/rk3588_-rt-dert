# 对话上下文摘要（2026-08-12，供后续对话续接）

## 项目与环境

- RK3588（LKD3588）端侧目标检测：RT-DETR-R18 INT8，C++17，CMake，RGA/DMA 零拷贝，Ubuntu 22.04。
- 本地 workspace：`C:\Users\zky\Desktop\experimental code 1\毕设-RK\【毕设】-RK3588\rt-detr-rk3588\Dmsj_毕设`（源码/文档均为 UTF-8）。
- 板端：`ssh -i ~/.ssh/codex_board_ed25519 neardi@192.168.2.234`；workspace 根 `/home/neardi/Workspace_Codex/`，写操作仅限该目录；原版参考系统 `/home/neardi/Workspace_Dmsj/DMSJ_Design/rt-detr-rk3588-deploy/`（只读/执行，用户授权）。
- 每次 SSH 登录后必须执行 5 条 performance governor 命令（AGENTS.md「板端性能释放」）；板子重启后 governor 会重置。
- 模型：`models/RT-DETR-RK3588-Models/.rknn/rtdetr_i8.rknn`（10 类 VisDrone；输出未归一化，阈值有效范围 > -1）。
- 测试素材：`cars.mp4`(1280×720)、`uav.jpg`(1360×765)、VisDrone 测试图(1920×1080)、录屏 mp4(480×332)。

## 关键规则（AGENTS.md，必须遵守）

- 板端先迭代 → 达成指标后询问用户是否上传入库；同意则更新 README + git 提交推送；拒绝则与下个同意任务一起入库。
- 指标驱动（METRICS.md），改动前后必须同指标对比；uncle-bob：新代码配套单测并如实报告（覆盖率/变异工具未配置，未伪造）。
- 权限：板端只读指令放权；编写/删改/安装必须先询问。
- 双基准参数：最佳性能 `-p 2 -n 14 -P 3 -c -0.13f`；显著降 CPU/内存甜点位 `-p 2 -n 8 -P 3 -c -0.13f`。
- SSH 引号/特殊字符注意事项见 `SSH交互注意事项.md`（执行 ssh 前必须自检）。

## 任务状态（TODOlist.md）

| # | 任务 | 状态 |
|---|------|------|
| 1 | 图片输入检测 + 检测一致性修复 | ✅ 已上传（e8308ab） |
| 2 | 日志等级化 `-G/--DEBUG` | ✅ 已上传（bc49c84） |
| 3 | RGA_DMA 链路分析优化（迭代 1+2） | ✅ 完成，本次上传 |
| 4 | GStreamer + RK MPP 硬解/硬编 | ⏸ 用户暂停，未开始 |
| 5 | 预处理/后处理提速（排除 NPU） | ⏸ 未开始 |

## 关键技术结论

- Bug1：输出张量误选（按名称选到 4200 元素 `pred_logits`，正确为 3000 元素 logits）→ 改为形状匹配（300×4 / 300×10，最后匹配者胜），与原版一致。
- Bug2：RGA 源 stride 对齐污染（1360 宽 → DRM stride 4096，480 宽 → 1472，wstride=width 无法表达）→ mat 输入改走 RGA virt→DMA（连续内存紧致源）；相机 DMA→DMA 用 wstride=实际 stride/bpp，非整除自动回退 CPU 逐行读取（模拟测试通过）。
- 图片模式用法：`-i/--image <图片> -o <输出.jpg> -c 阈值`（与 `-v` 互斥；图片模式忽略 -p/-n/-P）。
- 日志：`-G 0`=仅错误+运行报告；`-G 1`=Main；`-G 2`=+RKNN；…；`-G 8`=全部；ERROR 恒打印。
- 指标（cars.mp4）：最佳性能 15.8 FPS / CPU ~430% / RSS ~1500MB；甜点位 15.3 FPS / CPU ~365% / RSS ~953MB；预处理（RGA virt→DMA）~5.5ms；单测 15/15。
- 环形缓冲（消除 orig_img.clone）：已评估，收益为整机 CPU -1~2%、内存稳定化；用户认可列为低优先级，暂不实现。

## 待办 / 注意事项

- 任务 3 上传完成后，任务 4 等待用户指令再启动。
- 板端仓库工作区仍有未提交改动（内容与本地一致），如需整理需用户同意。
- 每次 ssh/scp 前对照 `SSH交互注意事项.md` 自检（PowerShell 单引号包裹远端命令、bash 引号用 `''` 转义、grep 用 `-e`、scp 目标单字符串、检查退出码）。
